# 프론트엔드 TourAPI 신분류 필터 매핑

기준: 한국관광공사 `신분류체계정보 관광타입정보 연계 정의서.xlsx`

- `찜한장소`: 개인 찜 컬렉션 조회이며 TourAPI 분류가 아니다.
- `선수추천`: TourAPI 장소 ID에 별도 DB 큐레이션을 연결한다.
- `Best`, `OTHER`: 사용하지 않는다.

## 검색바 고정 카테고리

| 앱 표시 | 분류 조건 | 비고 |
| --- | --- | --- |
| 찜한장소 | 개인 찜 컬렉션 | 로그인 필요 |
| 선수추천 | DB 큐레이션 | 구장·선수별 필터 |
| 음식점 | `FD` | 카페 `FD05`는 별도 탭 |
| 카페 | `FD05` | 카페·찻집·기타음료점 |
| 액티비티 | `LS` + 지정 시설 코드 | `VE10` 전체 자동 추천은 제외 유지 |
| 관광지 | `EX`, `HS`, `NA`, `VE` | 전시 `VE07`은 별도 탭 |
| 축제 | `EV01`, `EV030100` | 축제 및 전시회 |
| 전시 | `VE07` | 전시시설 |
| 쇼핑 | `SH` | 시내면세점만 사용 |

## 음식·카페

| 그룹 | 앱 표시 | 공식 명칭 | 코드 |
| --- | --- | --- | --- |
| 유형 | 한식 | 한식(관광식당·모범음식점) | `FD01` |
| 유형 | 중식 | 중식 | `FD020100` |
| 유형 | 일식 | 일식 | `FD020200` |
| 유형 | 양식 | 서양식 | `FD020300` |
| 간편식 | 김밥 분식 | 김밥 분식 | `FD030400` |
| 간편식 | 치킨 | 치킨 | `FD030300` |
| 간편식 | 피자 | 피자·햄버거·샌드위치 및 유사음식 | `FD030200` |
| 간편식 | 제과 | 제과 | `FD030100` |
| 주점 | 바/펍 | 바/펍 | `FD040100` |
| 주점 | 생맥주전문점 | 생맥주전문점 | `FD040200` |
| 주점 | 전통주 | 전통주/민속주점 | `FD040400` |
| 카페 | 카페 | 카페 | `FD050100` |
| 카페 | 찻집 | 찻집 | `FD050200` |
| 카페 | 기타음료 | 기타음료점 | `FD050300` |

## 액티비티

| 그룹 | 앱 표시 | 공식 명칭 | 코드 |
| --- | --- | --- | --- |
| 시설 | 스포츠센터 | 스포츠센터·수련시설 | `VE100200` |
| 시설 | 워터파크 | 워터파크 | `VE020200` |
| 시설 | 동물원 | 동물원 | `VE020300` |
| 시설 | 수족관/아쿠아리움 | 수족관/아쿠라리움 | `VE020400` |
| 시설 | 천문대 | 천문대 | `VE020500` |
| 시설 | 둘레길 | 둘레길 | `VE040300` |
| 육상 | 인라인 | 인라인 | `LS010100` |
| 육상 | 카트 | 카트 | `LS010300` |
| 육상 | 골프 | 골프 | `LS010400` |
| 육상 | 경마 | 경마 | `LS010500` |
| 육상 | 승마 | 승마 | `LS010700` |
| 육상 | 스키/스노우보드 | 스키/스노보드 | `LS010800` |
| 육상 | 스케이트 | 스케이트 | `LS010900` |
| 육상 | 썰매장 | 썰매장 | `LS011000` |
| 육상 | 사격장 | 사격장 | `LS011200` |
| 육상 | 암벽등반 | 암벽등반 | `LS011300` |
| 육상 | 번지점프 | 번지점프 | `LS011800` |
| 레저 | 제트스키 | 윈드서핑/제트스키 | `LS020100` |
| 레저 | 카약 | 카약/카누 | `LS020200` |
| 레저 | 요트 | 요트 | `LS020300` |
| 레저 | 스노쿨링 | 스노쿨링/스킨스쿠버다이빙 | `LS020400` |
| 레저 | 낚시 | 민물낚시·바다낚시 | `LS020500`, `LS020600` |
| 레저 | 수상자전거 | 수상자전거 | `LS021000` |
| 항공 | 스카이다이빙 | 스카이다이빙 | `LS030100` |
| 항공 | 패러글라이딩 | 헹글라이딩/패러글라이딩 | `LS030300` |
| 항공 | 열기구 | 열기구 | `LS030400` |

## 관광지

| 앱 표시 | 공식 명칭 | 코드 |
| --- | --- | --- |
| 전통체험 | 전통체험 | `EX01` |
| 공예체험 | 공예체험 | `EX02` |
| 농어촌체험 | 농·산·어촌 체험 | `EX03` |
| 템플스테이 | 템플스테이 | `EX040100` |
| 웰니스관광 | 웰니스관광 | `EX05` |
| 유람선 | 유람선/잠수함관광 | `EX070100` |
| 역사 유적지 | 역사유적지 | `HS01` |
| 역사 유물 | 역사유물 | `HS02` |
| 자연관광 | 자연관광 | `NA` |
| 랜드마크 | 랜드마크관광 | `VE01` |
| 공원 | 도시공원 | `VE03` |
| 문화거리 | 골목길·문화거리 | `VE040100` |

## 축제·전시

| 그룹 | 앱 표시 | 공식 명칭 | 코드 |
| --- | --- | --- | --- |
| 축제 | 문화관광 | 문화관광축제 | `EV010100` |
| 축제 | 문화예술 | 문화예술축제 | `EV010200` |
| 축제 | 지역특산물 | 지역특산물축제 | `EV010300` |
| 축제 | 전통역사 | 전통역사축제 | `EV010400` |
| 축제 | 생태자연 | 생태자연축제 | `EV010500` |
| 축제 | 전시회 | 전시회 | `EV030100` |
| 전시 | 박물관 | 박물관 | `VE070100` |
| 전시 | 기념관 | 기념관 | `VE070200` |
| 전시 | 전시관 | 전시관 | `VE070300` |
| 전시 | 과학관 | 과학관 | `VE070500` |
| 전시 | 미술관 | 미술관/화랑 | `VE070600` |

## 쇼핑

| 앱 표시 | 공식 명칭 | 코드 |
| --- | --- | --- |
| 백화점 | 백화점 | `SH010100` |
| 쇼핑몰 | 복합쇼핑몰 | `SH020100` |
| 아울렛 | 아웃렛 | `SH020200` |
| 대형마트 | 대형마트 | `SH030100` |
| 면세점 | 시내면세점 | `SH040200` |
| 공방 | 공방/공예품점 | `SH050100` |
| 기념품 | 관광기념품/특산물판매점 | `SH050300` |
| 시장 | 시장 | `SH06` |

## API 요청 규칙

프론트는 원시 신분류 코드 대신 `filterId` 사용을 권장한다.

```http
GET /api/v1/tour/filter-options
GET /api/v1/tour/search?keyword=서울&filterId=JAPANESE
GET /api/v1/tour/nearby?longitude=127.0&latitude=37.5&filterId=FISHING
```

백엔드는 `FISHING`처럼 여러 코드가 필요한 필터를 병렬 조회한 뒤
`placeId`로 중복을 제거한다. `filterId`는 `category` 또는
`lclsSystem1/2/3`과 동시에 보낼 수 없다.

원시 코드 조회는 관리·디버깅 호환용으로 유지한다.

- L1: `lclsSystem1=FD`
- L2: `lclsSystem1=FD&lclsSystem2=FD05`
- L3: `lclsSystem1=FD&lclsSystem2=FD05&lclsSystem3=FD050100`
- 낚시처럼 복수 L3가 필요한 필터는 백엔드에서 하나의 앱 필터 ID로
  묶어 제공하고 프론트가 여러 API를 직접 합치지 않게 한다.

## 프론트 전달 계약

### 고정 탭과 데이터 출처

| 탭 | 호출 대상 | 프론트 표시 기준 |
| --- | --- | --- |
| 찜한장소 | 찜 컬렉션 API | 내부 `Place` 응답 사용 |
| 선수추천 | `GET /api/v1/tour/player-picks?stadiumId=...` | 구장 필수, 선수 이름 선택 필터 |
| 음식점~쇼핑 | `/tour/search` 또는 `/tour/nearby` | 위 표의 신분류 코드 사용 |

- `OTHER`와 `Best` 탭은 제공하지 않는다.
- 코드의 한글 표시는 이 문서를 프론트의 고정 표시표로 사용한다.
  TourAPI 원문 명칭을 화면에서 임의로 다시 번역하지 않는다.
- 복수 코드를 하나로 묶은 필터는 프론트가 여러 결과를 직접 합치지
  않고 백엔드의 통합 필터 계약을 사용한다.

### 선수추천 Firestore 계약

컬렉션: `playerPlaceRecommendations`

| 필드 | 형식 | 설명 |
| --- | --- | --- |
| 문서 ID | 자동 생성 | 응답의 `playerPickId` |
| `stadiumId` | string | 구장 필터 |
| `playerName` | string | 화면에 표시할 선수명 |
| `placeId` | `tour_{contentId}` | 추천 장소 내부 ID |
| `placeSnapshot` | Place | 외부 API 장애 시 사용할 저장 시점 장소 정보 |
| `createdAt` | timestamp | 등록 시각 |
| `updatedAt` | timestamp | 마지막 갱신 시각 |

선수추천은 `Place.source`를 변경하지 않는다. 장소 원천은 계속
`TOUR_API`이며, 선수 추천 여부만 별도 DB 문서로 관리한다.

장소명·주소로 정리한 데이터를 입력할 때는 먼저 dry-run으로 매칭을
확인한다.

```bash
uv run python -m scripts.seed_player_picks --input samples/player_picks/player_picks.json
```

출력된 장소명·주소·`tour_` ID를 사람이 확인한 뒤에만 저장한다.

```bash
uv run python -m scripts.seed_player_picks --input samples/player_picks/player_picks.json --write
```

동점 후보나 이름·주소가 충분히 일치하지 않는 항목은 자동 저장하지
않는다. 입력 JSON에 확정한 `placeId`를 직접 적으면 재검색 없이 해당
TourAPI 상세를 저장한다.

### 여행 설문 계약

여행 생성·수정 요청은 다음 두 값만 받는다.

| 필드 | 후보 값 | 기본값 |
| --- | --- | --- |
| `travelStyle` | `RELAXED`, `BALANCED`, `EXPLORER` | `BALANCED` |
| `scheduleDensity` | `LIGHT`, `MODERATE`, `DENSE` | `MODERATE` |

`LIGHT`, `MODERATE`, `DENSE`는 하루 자동 추천 상한을 각각 2, 3, 5개로
적용한다. `RELAXED`는 짧은 우회와 넉넉한 잔여시간을 우선하고,
`EXPLORER`는 카테고리 다양성과 적극적인 빈 시간 활용을 우선한다.
식사 추천과 경기장 40분 전 도착 규칙은 스타일보다 먼저 적용한다.

### 추천 후보와 이동거리

- 일정 생성 전 후보: `GET /api/v1/trips/{tripId}/recommendation-candidates`
- 일정 Item의 이전 장소부터 거리: `travelDistanceMetersFromPrevious`
- 일정 전체 이동거리: `totalTravelDistanceMeters`
- 거리는 Kakao가 반환한 실제 경로 거리를 우선하고, 외부 경로 조회가
  실패하면 직선거리 기반 추정치를 사용한다.
